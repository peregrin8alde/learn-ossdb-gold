#baseURL = "http://localhost/"
title = 'OSS-DB Gold 学習資材'
relativeURLs = true

# ディレクトリ指定では index.html を自動で開かない場合向け
uglyurls = true

enableRobotsTXT = true

# Hugo allows theme composition (and inheritance). The precedence is from left to right.
theme = ["docsy"]

# Will give values to .Lastmod etc.
enableGitInfo = false

# Language settings
contentDir = "content/ja"
defaultContentLanguage = "ja"
defaultContentLanguageInSubdir = false
# Useful when translating.
enableMissingTranslationPlaceholders = true

# Comment out to disable taxonomies in Docsy
# disableKinds = ["taxonomy", "taxonomyTerm"]

# Enable syntax highlighting and copy buttons on code blocks with Prism
#prism_syntax_highlighting = true

# You can add your own taxonomies
[taxonomies]
tag = "tags"
category = "categories"

[params.taxonomy]
# set taxonomyCloud = [] to hide taxonomy clouds
taxonomyCloud = ["tags", "categories"] 

# If used, must have same lang as taxonomyCloud
taxonomyCloudTitle = ["Tag Cloud", "Categories"] 

# set taxonomyPageHeader = [] to hide taxonomies on the page headers
taxonomyPageHeader = ["tags", "categories"] 


# Highlighting config
pygmentsCodeFences = true
pygmentsUseClasses = false
# Use the new Chroma Go highlighter in Hugo.
pygmentsUseClassic = false
#pygmentsOptions = "linenos=table"
# See https://help.farbox.com/pygments.html
pygmentsStyle = "tango"

# Configure how URLs look like per section.
[permalinks]
blog = "/:section/:year/:month/:day/:slug/"

## Configuration for BlackFriday markdown parser: https://github.com/russross/blackfriday
[blackfriday]
plainIDAnchors = true
hrefTargetBlank = true
angledQuotes = false
latexDashes = true

# Image processing configuration.
[imaging]
resampleFilter = "CatmullRom"
quality = 75
anchor = "smart"

[services]
[services.googleAnalytics]
# Comment out the next line to disable GA tracking. Also disables the feature described in [params.ui.feedback].
#id = "UA-00000000-0"

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
  [markup.highlight]
      # See a complete list of available styles at https://xyproto.github.io/splash/docs/all.html
      style = "tango"
      # Uncomment if you want your chosen highlight style used for code blocks without a specified language
      # guessSyntax = "true"
  [markup.asciidocExt]
    # Don’t change this unless you know what you are doing.
    backend = 'html5'

    # Currently, the only document type supported in Hugo is `article`.
    # book と比べて簡素にする必要があるので構成に注意
    doctype = 'article'

    # Possible extensions are 
    #   asciidoctor-html5s, asciidoctor-bibtex, asciidoctor-diagram, 
    #   asciidoctor-interdoc-reftext, asciidoctor-katex, asciidoctor-latex, 
    #   asciidoctor-mathematical, asciidoctor-question, asciidoctor-rouge.
    extensions = []

    # Output an embeddable document, which excludes the header, the footer, 
    #   and everything outside the body of the document. 
    # Don’t change this unless you know what you are doing.
    noHeaderOrFooter = true

    # Safe mode level unsafe, safe, server or secure. Don’t change this unless you know what you are doing.
    safeMode = 'unsafe'

    # Auto-number section titles.
    sectionNumbers = false

    # Verbosely print processing information and configuration file checks to stderr.
    verbose = false

    # Include backtrace information on errors.
    trace = false

    # The minimum logging level that triggers a non-zero exit code (failure).
    failureLevel = 'fatal'

    # Sets the working directory to be the same as that of the AsciiDoc file being processed, 
    # so that include will work with relative paths. 
    # This setting uses the asciidoctor cli parameter --base-dir and attribute outdir=. 
    # For rendering diagrams with asciidoctor-diagram, workingFolderCurrent must be set to true.
    workingFolderCurrent = false

    # By default, Hugo removes the table of contents generated by Asciidoctor and provides it through the built-in variable `.TableOfContents`
    # to enable further customization and better integration with the various Hugo themes. 
    # This option can be set to true to preserve Asciidoctor’s TOC in the generated page.
    preserveTOC = false

    # Variables to be referenced in your AsciiDoc file. This is a list of variable name/value maps. 
    # See Asciidoctor’s attributes.
    #   https://asciidoctor.org/docs/asciidoc-syntax-quick-reference/#attributes-and-substitutions
    [markup.asciidocExt.attributes]
      imagesdir = 'static/images'
      appendix-caption = '付録'
      caution-caption = '注意'
      example-caption = '例'
      figure-caption = '図'
      important-caption = '重要'
      note-caption = '注記'
      table-caption = '表'
      tip-caption = '補足'
      warning-caption = '警告'
      data-uri = true
      icons = 'font'
      # hugo では Asciidoctor のスタイルは適用されない（ `--no-header-footer` ）ので工夫が必要
      # Asciidoctor と合わせる必要がなければ、docsy に同梱されている prism の js を直接利用した方が早そう
      # 参照 : layouts/partials/head.html highlight/highlightjs.html / prism.html
      # source-highlighter = 'highlight.js'


# Everything below this are Site Params

# Comment out if you don't want the "print entire section" link enabled.
[outputs]
section = ["HTML", "print", "RSS"]

[params]
copyright = "Okada Hayato."
#privacy_policy = "https://policies.google.com/privacy"

# First one is picked as the Twitter card image if not set on page.
# images = ["images/project-illustration.png"]

# Menu title if your navbar has a versions selector to access old versions of your site.
# This menu appears only if you have at least one [params.versions] set.
version_menu = "Releases"

# Flag used in the "version-banner" partial to decide whether to display a 
# banner on every page indicating that this is an archived version of the docs.
# Set this flag to "true" if you want to display the banner.
archived_version = false

# The version number for the version of the docs represented in this doc set.
# Used in the "version-banner" partial to display a version number for the 
# current doc set.
version = "0.0"

# A link to latest version of the docs. Used in the "version-banner" partial to
# point people to the main doc site.
#url_latest_version = "https://example.com"

# Repository configuration (URLs for in-page links to opening issues and suggesting changes)
github_repo = "https://gitlab.com/peregrin8alde-grp/learning/ossdb-gold"
# An optional link to a related project repo. For example, the sibling repository where your product code lives.
#github_project_repo = "https://github.com/google/docsy"

# Specify a value here if your content directory is not in your repo's root directory
github_subdir = "site"

# Uncomment this if you have a newer GitHub repo with "main" as the default branch,
# or specify a new value if you want to reference another branch in your GitHub links
github_branch= "develop"

# Google Custom Search Engine ID. Remove or comment out to disable search.
#gcs_engine_id = "d72aa9b2712488cc3"

# Enable Algolia DocSearch
algolia_docsearch = false

# Enable Lunr.js offline search
offlineSearch = true

# Enable syntax highlighting and copy buttons on code blocks with Prism
prism_syntax_highlighting = false

# User interface configuration
[params.ui]
#  Set to true to disable breadcrumb navigation.
breadcrumb_disable = false
# Set to true to disable the About link in the site footer
footer_about_disable = false
# Set to false if you don't want to display a logo (/assets/icons/logo.svg) in the top navbar
navbar_logo = true
# Set to true if you don't want the top navbar to be translucent when over a `block/cover`, like on the homepage.
navbar_translucent_over_cover_disable = false
# Enable to show the side bar menu in its compact state.
sidebar_menu_compact = false
# Set to true to hide the sidebar search box (the top nav search box will still be displayed if search is enabled)
sidebar_search_disable = false

# Adds a H2 section titled "Feedback" to the bottom of each doc. The responses are sent to Google Analytics as events.
# This feature depends on [services.googleAnalytics] and will be disabled if "services.googleAnalytics.id" is not set.
# If you want this feature, but occasionally need to remove the "Feedback" section from a single page,
# add "hide_feedback: true" to the page's front matter.
[params.ui.feedback]
enable = false
# The responses that the user sees after clicking "yes" (the page was helpful) or "no" (the page was not helpful).
yes = 'Glad to hear it! Please <a href="https://github.com/USERNAME/REPOSITORY/issues/new">tell us how we can improve</a>.'
no = 'Sorry to hear that. Please <a href="https://github.com/USERNAME/REPOSITORY/issues/new">tell us how we can improve</a>.'

# Adds a reading time to the top of each doc.
# If you want this feature, but occasionally need to remove the Reading time from a single page, 
# add "hide_readingtime: true" to the page's front matter
[params.ui.readingtime]
enable = false

[params.links]
# End user relevant links. These will show up on left side of footer and in the community page if you have one.


